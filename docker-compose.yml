services:
  nextjs-app:
    build: .
    container_name: nextjs-portfolio
    networks:
      - web-gateway
    expose:
      - "3000"
    restart: always
    environment:
      - NODE_ENV=production


networks:
  web-gateway:
    external: true